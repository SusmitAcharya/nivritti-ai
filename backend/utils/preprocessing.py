import pandas as pd

def prep_forecast(df: pd.DataFrame) -> pd.DataFrame:
    # TODO: parse dates, resample, fill missing
    return df

def prep_segments(df: pd.DataFrame) -> pd.DataFrame:
    # TODO: scale / encode required cols
    return df

def prep_churn(df: pd.DataFrame) -> pd.DataFrame:
    # TODO: feature engineering
    return df